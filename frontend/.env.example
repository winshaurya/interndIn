# Frontend environment example for Vite (.env.example)
# Copy to .env.local or set via hosting provider (e.g., Vercel) as environment variables.

VITE_API_URL=http://localhost:5000
VITE_APP_NAME=interndin
# Example: VITE_GOOGLE_ANALYTICS_ID=G-XXXXXXXX

# Add other frontend-specific env vars below
